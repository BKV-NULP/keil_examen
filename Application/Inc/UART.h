/*#include "debug_frmwrk.h"

enum State {
	HELP, 
	PINX, 
	PINXexactlyY,
	ADC,
	ADCcount,
	TIME,
	DAC,
	STORE,
	PWMXexactlyY,
	PWMX
}; 

extern uint8_t port;
extern uint8_t pin;
extern uint8_t data;

enum State ReadUart();
char* DeleteSpace(char* str, uint8_t index);
Bool comparite(char* str1,char* str2);
void help();*/